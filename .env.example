# Copy to .env and adjust if local ports conflict with other projects.
# These values are used by docker-compose.yml host port mappings.

HOST_FRONTEND_PORT=15173
HOST_BACKEND_PORT=18080
HOST_AI_PORT=18000
